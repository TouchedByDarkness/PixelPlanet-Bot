import { Options as BotOptions } from './Bot'
import { Vec2 } from './common'

export const STRATEGY = 'zipper2';

export const BOT_OPTIONS: BotOptions = {
	smartPlace: true,
	showErrors: true
}

export const STOP_ON_CAPTCHA = true;

export const WINDOW_POS: Vec2 = [16, 16];